# Monsters of Jungle Website

## Deployment Instructions

1. Install dependencies: `npm install`
2. Build the project: `npm run build`
3. Host the `dist` folder

- After building, all production files will be in the dist directory.
- Serve the contents of the dist folder with your web server.

# SJ-Web-Crate Plugin
### How it all works

This plugin takes in all the content and prepares the data that vite uses to generate the website individual pages.

![](plugins/sj-web-crate/branding.png)
